import styled from '@emotion/styled';

export const Container = styled.div`
  min-width: 900px;
`;

export const HeaderContainer = styled.div`
  background-color: ${(props) => props.theme.color.blueHover};
`;
